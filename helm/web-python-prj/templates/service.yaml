apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-python-prj.fullname" . }}
  labels:
    {{- include "web-python-prj.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "web-python-prj.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
